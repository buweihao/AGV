<UserControl x:Class="BasicRegionNavigation.Controls.StatusBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicRegionNavigation.Controls"
             mc:Ignorable="d" 
             Background ="Transparent"
             d:DesignHeight="100" d:DesignWidth="300">

    <Border CornerRadius="4" Background="#90212121" Padding="4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Rectangle Grid.Column="0" Fill="{Binding StatusColor, RelativeSource={RelativeSource AncestorType=UserControl}}" RadiusX="2" RadiusY="2"/>

            <Grid Grid.Column="1" Margin="5,0,4,0" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Viewbox Stretch="Uniform" HorizontalAlignment="Left" StretchDirection="DownOnly">
                        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="18" FontWeight="SemiBold" Foreground="White" 
                   ToolTip="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </Viewbox>
                </Grid>

                <Grid Grid.Row="1" Margin="0,2,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" FontSize="13" VerticalAlignment="Bottom">
                        <Run Text="产能:" Foreground="#CCCCCC"/>
                        <Run Text="{Binding Output, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="White" FontWeight="Bold"/>
                    </TextBlock>

                    <TextBlock Grid.Column="1" 
                               Text="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               Foreground="{Binding StatusColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               FontSize="15" FontWeight="Bold" 
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Right"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>